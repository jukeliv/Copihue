# Copihue
- Add ability to include other scripts
- Add structs
- Add ability to set a variable value to a function return value
- Add documentation
# System to include other scripts
- Made a file that especifies what are going to be the scripts added into the file.
  also the main file of the program, and then smash all together into one file, separating the C++ includes
  and the code, and put every include 
# Compiler
- Add compilation optimizations ( we don't need the code to be to much human readable lol, just optimize it )